# Rootnote

Rootnote gives agents efficient, durable access to context through maintainable Markdown knowledge graphs stored in `.vault/`.

Use `node src/cli.mjs <command> [--repoRoot path] [--json]` for the CLI, or install through this repository's local marketplace to use the MCP tools and skill.